import React from "react";
import { searchResultQueryLabel } from "../../Labels";

const SearchResults = () => {
  return (
    <div>
      <h2> {searchResultQueryLabel} </h2>
    </div>
  );
};

export default SearchResults;
